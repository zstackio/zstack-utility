version: 0.1
maxsizemb: 1000 # the maximum size of an image blob in MB
storage:
    cache:
        layerinfo: inmemory
    filesystem:
        rootdirectory: /tmp/zstore
http:
    addr: :8000
tls:
    key: server_data/private_key.pem
    trusted: server_data/trusted_clients.pem
