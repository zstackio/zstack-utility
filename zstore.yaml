version: 0.1
maxsize: 10g # the maximum size of an image blob
storage:
    cache:
        layerinfo: inmemory
    filesystem:
        rootdirectory: /tmp/zstore
http:
    addr: :8000
tls:
    key: server_data/private_key.pem
    trusted: server_data/trusted_clients.pem
