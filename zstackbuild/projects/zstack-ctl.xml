<?xml version="1.0" encoding="UTF-8"?>

<project name="zstackctl builder" basedir="../">
    <property name="zstackctl.bdir" location="${build.dir}/zstackctl" />
    <property name="zstackctl.py26.virtualenv.file" location="${virtualenv.dir}/zstackctl-virtualenv-py26.tar.bz" />
    <property name="zstackctl.py26.virtualenv.dir" location="${virtualenv.dir}/zstackctl" />
    <property name="zstackctl.py27.virtualenv.file" location="${chroot.virtualenv.dir}/zstackctl-virtualenv-py27.tar.bz" />
    <property name="zstackctl.p27.virtualenv.chroot.dir" location="/virtualenv/zstackctl" />
    <property name="zstackctl.p27.virtualenv.dir" location="${chroot.virtualenv.dir}/zstackctl" />

    <target name="build-ctl" >
        <makeDir dir="${zstackctl.bdir}" />

        <checkProperty property="zstackctl.source" />
        <checkFile file="${zstackctl.source}" />
        <checkFile file="${zstackctl.source}/setup.py" />

        <delete dir="${zstackctl.source}/dist" />

        <exec executable="python" dir="${zstackctl.source}" failonerror="true">
            <arg value="setup.py" />
            <arg value="sdist" />
        </exec>

        <copy todir="${zstackctl.bdir}/">
            <fileset dir="${zstackctl.source}/">
                <exclude name="**/*~" />
                <include name="dist/*.tar.gz" />
            </fileset>
        </copy>
    </target>

    <target name="assemble-ctl">
        <copy todir="${tools.dir}">
            <fileset dir="${zstackctl.bdir}/dist">
                <include name="**/*" />
            </fileset>
        </copy>

        <makeDir2 dir="${zstackctl.py26.virtualenv.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv}" />
            <arg value="${zstackctl.py26.virtualenv.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${zstackctl.py26.virtualenv.dir}" />
            <arg value="${tools.dir}/zstackctl*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${zstackctl.py26.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${zstackctl.py26.virtualenv.file}" />
            <arg value="zstackctl" />
        </exec>

        <copy file="${zstackctl.py26.virtualenv.file}" todir="${tools.dir}" />

        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${zstackctl.p27.virtualenv.chroot.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${zstackctl.p27.virtualenv.chroot.dir}" />
            <arg value="${tools.dir}/zstackctl*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${zstackctl.p27.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${chroot.virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${zstackctl.py27.virtualenv.file}" />
            <arg value="zstackctl" />
        </exec>

        <copy file="${zstackctl.py27.virtualenv.file}" todir="${tools.dir}" />
        <copy file="${zstackctl.binfile}" todir="${tools.dir}" />
    </target>
</project>
