<?xml version="1.0" encoding="UTF-8"?>

<project name="zstack-cli builder" basedir="../">
    <property name="zstackcli.bdir" location="${build.dir}/zstackcli" />
    <property name="zstackcli.py26.virtualenv.file" location="${virtualenv.dir}/zstackcli-virtualenv-py26.tar.bz" />
    <property name="zstackcli.py26.virtualenv.dir" location="${virtualenv.dir}/zstackcli" />
    <property name="zstackcli.py27.virtualenv.file" location="${chroot.virtualenv.dir}/zstackcli-virtualenv-py27.tar.bz" />
    <property name="zstackcli.p27.virtualenv.chroot.dir" location="/virtualenv/zstackcli" />
    <property name="zstackcli.p27.virtualenv.dir" location="${chroot.virtualenv.dir}/zstackcli" />

    <target name="build-cli" >
        <makeDir dir="${zstackcli.bdir}" />

        <checkProperty property="zstackcli.source" />
        <checkFile file="${zstackcli.source}" />
        <checkFile file="${zstackcli.source}/setup.py" />

        <delete dir="${zstackcli.source}/dist" />

        <exec executable="python" dir="${zstackcli.source}" failonerror="true">
            <arg value="setup.py" />
            <arg value="sdist" />
        </exec>

        <copy todir="${zstackcli.bdir}/">
            <fileset dir="${zstackcli.source}/">
                <exclude name="**/*~" />
                <include name="dist/*.tar.gz" />
            </fileset>
        </copy>
    </target>

    <target name="assemble-cli">
        <copy todir="${tools.dir}">
            <fileset dir="${zstackcli.bdir}/dist">
                <include name="**/*" />
            </fileset>
        </copy>

        <makeDir2 dir="${zstackcli.py26.virtualenv.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv}" />
            <arg value="${zstackcli.py26.virtualenv.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${zstackcli.py26.virtualenv.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${zstackcli.py26.virtualenv.dir}" />
            <arg value="${tools.dir}/zstackcli*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${zstackcli.py26.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${zstackcli.py26.virtualenv.file}" />
            <arg value="zstackcli" />
        </exec>

        <copy file="${zstackcli.py26.virtualenv.file}" todir="${tools.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${zstackcli.p27.virtualenv.chroot.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${zstackcli.p27.virtualenv.chroot.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${zstackcli.p27.virtualenv.chroot.dir}" />
            <arg value="${tools.dir}/zstackcli*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${zstackcli.p27.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${chroot.virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${zstackcli.py27.virtualenv.file}" />
            <arg value="zstackcli" />
        </exec>

        <copy file="${zstackcli.py27.virtualenv.file}" todir="${tools.dir}" />
        <copy file="${zstackcli.binfile}" todir="${tools.dir}" />
    </target>
</project>
