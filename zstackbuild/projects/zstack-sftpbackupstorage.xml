<?xml version="1.0" encoding="UTF-8"?>

<project name="zstack-sftpbackupstorage builder" basedir="../">
    <property name="sftpbackupstorage.bdir" location="${build.dir}/sftpbackupstorage" />
    <property name="sftpbackupstorage.salt.dir" location="${salt.dir}/sftpbackupstorage/" />
    <property name="sftpbackupstorage.ansible.dir" location="${ansible.dir}/sftpbackupstorage/" />
    <property name="sftpbackupstorage.py26.virtualenv.file" location="${virtualenv.dir}/sftpbackupstorage-virtualenv-py26.tar.bz" />
    <property name="sftpbackupstorage.py26.virtualenv.dir" location="${virtualenv.dir}/sftpbackupstorage" />
    <property name="sftpbackupstorage.py27.virtualenv.file" location="${chroot.virtualenv.dir}/sftpbackupstorage-virtualenv-py27.tar.bz" />
    <property name="sftpbackupstorage.p27.virtualenv.chroot.dir" location="/virtualenv/sftpbackupstorage" />
    <property name="sftpbackupstorage.p27.virtualenv.dir" location="${chroot.virtualenv.dir}/sftpbackupstorage" />

    <target name="build-sftpbackupstorage" >
        <makeDir dir="${sftpbackupstorage.bdir}" />

        <checkProperty property="sftpbackupstorage.source" />
        <checkProperty property="sftpbackupstorage.serviceFile" />
        <checkFile file="${sftpbackupstorage.source}" />
        <checkFile file="${sftpbackupstorage.source}/setup.py" />
        <checkFile file="${sftpbackupstorage.serviceFile}" />

        <delete dir="${sftpbackupstorage.source}/dist" />

        <exec executable="python" dir="${sftpbackupstorage.source}" failonerror="true">
            <arg value="setup.py" />
            <arg value="sdist" />
        </exec>

        <copy todir="${sftpbackupstorage.bdir}/">
            <fileset dir="${sftpbackupstorage.source}/">
                <exclude name="**/*~" />
                <include name="dist/*.tar.gz" />
                <include name="salt/**/*" />
                <include name="ansible/**/*" />
            </fileset>
        </copy>
    </target>

    <target name="assemble-sftpbackupstorage">
        <copy file="${sftpbackupstorage.serviceFile}" todir="${sftpbackupstorage.salt.dir}" />

        <makeDir dir="${sftpbackupstorage.ansible.dir}" />

        <copy todir="${sftpbackupstorage.ansible.dir}">
            <fileset dir="${sftpbackupstorage.bdir}/ansible">
                <include name="**/*" />
            </fileset>
            <fileset dir="${sftpbackupstorage.bdir}/dist">
                <include name="**/*" />
            </fileset>
        </copy>

        <makeDir2 dir="${sftpbackupstorage.py26.virtualenv.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv}" />
            <arg value="${sftpbackupstorage.py26.virtualenv.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${sftpbackupstorage.py26.virtualenv.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${sftpbackupstorage.py26.virtualenv.dir}" />
            <arg value="${sftpbackupstorage.ansible.dir}/sftpbackupstorage*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${sftpbackupstorage.py26.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${sftpbackupstorage.py26.virtualenv.file}" />
            <arg value="sftpbackupstorage" />
        </exec>

        <copy file="${sftpbackupstorage.py26.virtualenv.file}" todir="${sftpbackupstorage.ansible.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${sftpbackupstorage.p27.virtualenv.chroot.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${sftpbackupstorage.p27.virtualenv.chroot.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${sftpbackupstorage.p27.virtualenv.chroot.dir}" />
            <arg value="${sftpbackupstorage.ansible.dir}/sftpbackupstorage*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${sftpbackupstorage.p27.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${chroot.virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${sftpbackupstorage.py27.virtualenv.file}" />
            <arg value="sftpbackupstorage" />
        </exec>

        <copy file="${sftpbackupstorage.py27.virtualenv.file}" todir="${sftpbackupstorage.ansible.dir}" />
        <copy file="${sftpbackupstorage.serviceFile}" todir="${sftpbackupstorage.ansible.dir}" />
    </target>
</project>
