<?xml version="1.0" encoding="UTF-8"?>

<project name="zstack-iscsi builder" basedir="../">
    <property name="iscsi.bdir" location="${build.dir}/iscsi" />
    <property name="iscsi.ansible.dir" location="${ansible.dir}/iscsi/" />
    <property name="iscsi.py26.virtualenv.file" location="${virtualenv.dir}/iscsi-virtualenv-py26.tar.bz" />
    <property name="iscsi.py26.virtualenv.dir" location="${virtualenv.dir}/iscsi" />
    <property name="iscsi.py27.virtualenv.file" location="${chroot.virtualenv.dir}/iscsi-virtualenv-py27.tar.bz" />
    <property name="iscsi.p27.virtualenv.chroot.dir" location="/virtualenv/iscsi" />
    <property name="iscsi.p27.virtualenv.dir" location="${chroot.virtualenv.dir}/iscsi" />

    <target name="build-iscsi" >
        <makeDir dir="${iscsi.bdir}" />

        <checkProperty property="iscsi.source" />
        <checkProperty property="iscsi.serviceFile" />
        <checkFile file="${iscsi.source}" />
        <checkFile file="${iscsi.source}/setup.py" />
        <checkFile file="${iscsi.serviceFile}" />

        <delete dir="${iscsi.source}/dist" />

        <exec executable="python" dir="${iscsi.source}" failonerror="true">
            <arg value="setup.py" />
            <arg value="sdist" />
        </exec>

        <copy todir="${iscsi.bdir}/">
            <fileset dir="${iscsi.source}/">
                <exclude name="**/*~" />
                <include name="dist/*.tar.gz" />
                <include name="ansible/**/*" />
            </fileset>
        </copy>
    </target>

    <target name="assemble-iscsi">
        <makeDir dir="${iscsi.ansible.dir}" />

        <copy todir="${iscsi.ansible.dir}">
            <fileset dir="${iscsi.bdir}/ansible">
                <include name="**/*" />
            </fileset>
            <fileset dir="${iscsi.bdir}/dist">
                <include name="**/*" />
            </fileset>
        </copy>
        <makeDir2 dir="${iscsi.py26.virtualenv.dir}" />
        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv}" />
            <arg value="${iscsi.py26.virtualenv.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${iscsi.py26.virtualenv.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv}" />
            <arg value="${iscsi.py26.virtualenv.dir}" />
            <arg value="${iscsi.ansible.dir}/iscsi*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${iscsi.py26.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${iscsi.py26.virtualenv.file}" />
            <arg value="iscsi" />
        </exec>

        <copy file="${iscsi.py26.virtualenv.file}" todir="${iscsi.ansible.dir}" />

        <exec executable="bash" failonerror="true">
            <arg value="${create.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${iscsi.p27.virtualenv.chroot.dir}" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${iscsi.p27.virtualenv.chroot.dir}" />
            <arg value="${zstacklib.ansible.dir}/zstacklib*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${install.lib.to.virtualenv.inchroot}" />
            <arg value="${centos7_chroot_env}" />
            <arg value="${iscsi.p27.virtualenv.chroot.dir}" />
            <arg value="${iscsi.ansible.dir}/iscsi*.tar.gz" />
        </exec>

        <exec executable="bash" failonerror="true">
            <arg value="${update.virtualenv}" />
            <arg value="${iscsi.p27.virtualenv.dir}" />
        </exec>

        <exec executable="tar" dir="${chroot.virtualenv.dir}" failonerror="true">
            <arg value="jcf" />
            <arg value="${iscsi.py27.virtualenv.file}" />
            <arg value="iscsi" />
        </exec>

        <copy file="${iscsi.py27.virtualenv.file}" todir="${iscsi.ansible.dir}" />
        <copy file="${iscsi.serviceFile}" todir="${iscsi.ansible.dir}" />
    </target>
</project>
